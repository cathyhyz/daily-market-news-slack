# Copy to .env and fill in (never commit .env)
# Optional: comma-separated RSS URLs (defaults used if unset)
# RSS_FEEDS=https://example.com/feed.xml,https://other.org/news.atom
GEMINI_API_KEY=
SLACK_WEBHOOK_URL=
